'''
``PyInstaller`` is a library that bundles a Python application and all its dependencies into a single package.
The user can run the packaged app without installing a Python interpreter or any modules.

So basically, it compile all your codes in the ``main.py``
and its dependencies into 1 single binary executable file
(like ``.exe`` files in Windows)

Installation:
    + pip install pyinstaller
    + uv pip install pyinstaller (for venv created by uv)
'''
